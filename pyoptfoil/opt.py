from .algorithms.de import DE


def _drag_reward(cl: float, re: float, m: float, alpha_range: tuple, xfoil_path: str = 'xfoil.exe', itermax: int = 100):
    pass


def opt(optimizer: DE, cl: float, re: float, m: float, alpha_range: tuple,
        xfoil_path: str = 'xfoil.exe', itermax: int = 100):
    pass
